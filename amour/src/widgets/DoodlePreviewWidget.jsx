import {useNavigate} from 'react-router-dom';import {useAppStore} from '../store/appStore'
export default function DoodlePreviewWidget(){const doodle=useAppStore(s=>s.doodles[0]),nav=useNavigate();return <button onClick={()=>nav(doodle?'/doodle':'/lockscreen-doodle')} className="glass-card tap h-full w-full p-3 text-left"><p className="text-[10px] tracking-[.16em] text-muted">🎨 DOODLE</p>{doodle?<img src={doodle.data} className="mt-2 h-24 w-full rounded-xl object-cover"/>:<div className="mt-3 grid h-24 place-items-center rounded-xl border border-dashed border-pink/30 text-center text-xs text-muted">Draw something ✨</div>}</button>}
