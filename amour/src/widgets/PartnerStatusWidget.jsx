import {useNavigate} from 'react-router-dom';import {useAppStore} from '../store/appStore';import Avatar from '../components/ui/Avatar'
export default function PartnerStatusWidget(){const {partner,partnerOnline,partnerMood}=useAppStore(),nav=useNavigate(),online=partnerOnline||partner?.online;return <button onClick={()=>nav('/profile')} className="glass-card tap h-full w-full p-4 text-left"><p className="text-[10px] tracking-[.15em] text-muted">PARTNER</p><div className="mt-3 flex items-center gap-2"><Avatar name={partner?.name||'Partner'} size="sm" online={online}/><div><b className="block text-sm">{partner?.name?.split(' ')[0]||'Partner'}</b><small className={online?'text-green-300':'text-muted'}>{online?'🟢 Online now':'⚫ Offline'}</small></div></div><p className="mt-3 text-xs text-muted"><span className="mr-1 text-xl">{partnerMood?.emoji||'💕'}</span>{partnerMood?.label||'Thinking of you'}</p></button>}
