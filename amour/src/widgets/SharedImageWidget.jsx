import {format} from 'date-fns';import {useNavigate} from 'react-router-dom';import {useAppStore} from '../store/appStore'
export default function SharedImageWidget(){const image=useAppStore(s=>s.sharedImages[0]),nav=useNavigate();return <button onClick={()=>nav('/images')} className="glass-card tap w-full overflow-hidden text-left">{image?<div className="relative"><img src={image.url} className="h-44 w-full object-cover"/><div className="absolute inset-x-0 bottom-0 bg-gradient-to-t from-black/70 p-3 text-xs">📷 from {image.sender||'your partner'} · {format(new Date(image.created_at),'MMM d')}</div></div>:<div className="m-3 grid h-32 place-items-center rounded-xl border border-dashed border-pink/30 text-center text-muted"><span className="text-sm"><b className="block text-4xl">📷</b>Share a photo</span></div>}</button>}
