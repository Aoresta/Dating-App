import {format} from 'date-fns';import {useState} from 'react';import {useNavigate} from 'react-router-dom';import {useAppStore} from '../store/appStore';import {AddMemoryModal} from '../pages/MemoriesPage'
export default function MemoriesStripWidget(){const memories=useAppStore(s=>s.memories),latest=memories[0],nav=useNavigate(),[open,setOpen]=useState(false),src=latest?.image_data||latest?.image_url;return <><div className="glass-card p-4"><header className="flex items-center gap-2"><b className="text-xs">📸 MEMORIES</b><span className="rounded-full bg-pink/15 px-2 py-0.5 text-[10px] text-pink-light">{memories.length}</span><button onClick={()=>setOpen(true)} className="tap ml-auto text-xs text-pink-light">+ Add</button></header>{latest?<button onClick={()=>nav('/memories')} className="tap mt-3 w-full text-left">{src?<img src={src} className="h-36 w-full rounded-xl object-cover"/>:<div className="grid h-28 place-items-center rounded-xl bg-gradient-to-br from-pink/30 to-purple/40 text-5xl">🌸</div>}<h3 className="display mt-2 text-lg">{latest.title}</h3><p className="line-clamp-1 text-xs text-muted">{latest.story}</p><time className="mt-1 block text-right text-[10px] text-pink-light">{format(new Date(latest.date||latest.created_at),'MMM d, yyyy')}</time></button>:<button onClick={()=>setOpen(true)} className="tap grid h-28 w-full place-items-center text-xs text-muted">Create your first memory 🌸</button>}</div>{open&&<AddMemoryModal onClose={()=>setOpen(false)}/>}</>}
