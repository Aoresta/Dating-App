import {differenceInCalendarDays,format,getDayOfYear} from 'date-fns';import {useAppStore} from '../store/appStore'
export default function DaysTogetherWidget(){const couple=useAppStore(s=>s.couple),days=couple?.start_date?differenceInCalendarDays(new Date(),new Date(couple.start_date)):0,years=Math.floor(days/365);return <div className="glass-card overflow-hidden p-5"><p className="text-[10px] uppercase tracking-[.2em] text-muted">Our little timeline</p><div className="mt-1 flex items-end gap-2"><b className="display gradient-text text-7xl leading-none">{days}</b><span className="mb-2 text-lg text-pink-light">days</span></div><p className="mt-3 text-sm">🔥 Still choosing each other, every day</p><div className="mt-4 h-1.5 rounded-full bg-white/10"><div className="h-full rounded-full bg-gradient-to-r from-pink to-purple" style={{width:`${getDayOfYear(new Date())/365*100}%`}}/></div><div className="mt-2 flex justify-between text-[10px] text-muted"><span>{couple?.start_date?format(new Date(couple.start_date),'MMM d, yyyy'):'Set your date'}</span><span>{years}y {days%365}d</span></div></div>}
