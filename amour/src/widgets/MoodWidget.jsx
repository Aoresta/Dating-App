import {useNavigate} from 'react-router-dom';import {useAppStore} from '../store/appStore'
export default function MoodWidget(){const {myMood,partnerMood}=useAppStore(),nav=useNavigate();return <button onClick={()=>nav('/mood')} className="glass-card tap h-full w-full p-4 text-center"><p className="text-[10px] tracking-[.18em] text-muted">MOOD</p><span className="mt-2 block text-4xl">{myMood?.emoji||'—'}</span><small className="text-muted">{myMood?.label||'Set yours'}</small><div className="my-3 h-px bg-purple/20"/><p className="text-xs"><span className="mr-1 text-xl">{partnerMood?.emoji||'—'}</span> Partner</p></button>}
