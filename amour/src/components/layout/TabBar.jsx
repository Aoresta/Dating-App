import { Heart, Home, MessageCircleHeart, Images, Smile, Sparkles } from 'lucide-react';import { Link, useLocation } from 'react-router-dom';import { motion } from 'framer-motion'
const tabs=[['/home','Home',Home],['/notes','Notes',MessageCircleHeart],['/mood','Mood',Smile],['/memories','Memories',Images],['/quiz','Quiz',Sparkles]]
export default function TabBar(){const {pathname}=useLocation();return <nav className="fixed bottom-0 left-0 right-0 z-30 border-t border-pink/15 bg-bg/95 px-2 pt-2 backdrop-blur-xl" style={{paddingBottom:'max(8px,env(safe-area-inset-bottom))'}}><div className="mx-auto flex max-w-md justify-between">{tabs.map(([path,label,Icon])=><Link key={path} to={path} className={`flex min-w-14 flex-col items-center gap-1 rounded-2xl px-2 py-1.5 text-[10px] transition ${pathname===path?'bg-pink/15 text-pink-light':'text-muted'}`}><motion.span whileTap={{scale:.9}}>{label==='Notes'?<Heart size={19}/>:<Icon size={19}/>}</motion.span>{label}</Link>)}</div></nav>}
