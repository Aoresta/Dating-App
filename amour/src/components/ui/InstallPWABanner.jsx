import { useState } from 'react';import { X } from 'lucide-react';import { usePWA } from '../../hooks/usePWA'
export default function InstallPWABanner(){const {canInstall,install}=usePWA();const [hide,setHide]=useState(localStorage.getItem('amour-install-dismissed'));if(!canInstall||hide)return null;return <div className="mx-auto mb-3 flex max-w-md items-center gap-2 rounded-2xl border border-pink/20 bg-surface/90 p-3 text-sm shadow-xl"><span>📲</span><b className="flex-1">Add Amour to Home Screen</b><button onClick={install} className="tap rounded-full bg-pink px-3 py-1 text-xs font-bold">Install</button><button onClick={()=>{localStorage.setItem('amour-install-dismissed','1');setHide(true)}}><X size={16}/></button></div>}
