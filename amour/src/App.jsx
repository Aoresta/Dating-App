import { Component,useEffect } from 'react';import { BrowserRouter,Navigate,Route,Routes } from 'react-router-dom';import { useAppStore } from './store/appStore';import { useNativeAuthRedirect } from './hooks/useNativeAuthRedirect';import { useAndroidWidgets } from './hooks/useAndroidWidgets';import SplashScreen from './pages/SplashScreen';import AuthPage from './pages/AuthPage';import PairingPage from './pages/PairingPage';import Dashboard from './pages/Dashboard';import NotesPage from './pages/NotesPage';import MoodPage from './pages/MoodPage';import MemoriesPage from './pages/MemoriesPage';import QuizPage from './pages/QuizPage';import DoodlePage from './pages/DoodlePage';import ImageSharePage from './pages/ImageSharePage';import ProfilePage from './pages/ProfilePage';import SettingsPage from './pages/SettingsPage';import LockscreenDoodle from './pages/LockscreenDoodle'
class ErrorBoundary extends Component{state={error:null};static getDerivedStateFromError(error){return{error}}render(){return this.state.error?<div className="grid min-h-screen place-items-center bg-bg p-8 text-center text-white"><div><span className="text-5xl">💔</span><h1 className="display mt-3 text-3xl">A little pause</h1><p className="mt-2 text-muted">Amour hit a snag. Refresh the page and your saved moments will still be here.</p><button onClick={()=>location.reload()} className="primary-btn mt-5">Refresh Amour</button></div></div>:this.props.children}}
function Protected({children}){const {user,demoMode,loading}=useAppStore();if(loading)return <div className="grid min-h-screen place-items-center bg-bg"><div className="skeleton h-20 w-20 rounded-3xl"/></div>;return user||demoMode?children:<Navigate to="/auth" replace/>}
const guard=x=><Protected>{x}</Protected>
export default function App(){const init=useAppStore(s=>s.initAuth);useNativeAuthRedirect();useAndroidWidgets();useEffect(()=>{init()},[init]);return <ErrorBoundary><BrowserRouter><Routes><Route path="/" element={<Navigate to="/home" replace/>}/><Route path="/splash" element={<SplashScreen/>}/><Route path="/auth" element={<AuthPage/>}/><Route path="/pair" element={guard(<PairingPage/>)}/><Route path="/home" element={guard(<Dashboard/>)}/><Route path="/notes" element={guard(<NotesPage/>)}/><Route path="/mood" element={guard(<MoodPage/>)}/><Route path="/memories" element={guard(<MemoriesPage/>)}/><Route path="/quiz" element={guard(<QuizPage/>)}/><Route path="/doodle" element={guard(<DoodlePage/>)}/><Route path="/lockscreen-doodle" element={guard(<LockscreenDoodle/>)}/><Route path="/images" element={guard(<ImageSharePage/>)}/><Route path="/profile" element={guard(<ProfilePage/>)}/><Route path="/settings" element={guard(<SettingsPage/>)}/><Route path="*" element={<Navigate to="/home" replace/>}/></Routes></BrowserRouter></ErrorBoundary>}
