import { useEffect } from 'react';import { useNavigate } from 'react-router-dom';import MeshBackground from '../components/ui/MeshBackground';import { useAppStore } from '../store/appStore'
export default function SplashScreen(){const nav=useNavigate();const {user,demoMode}=useAppStore();useEffect(()=>{const t=setTimeout(()=>nav(user||demoMode?'/home':'/auth'),2500);return()=>clearTimeout(t)},[user,demoMode,nav]);return <div className="grid min-h-screen place-items-center text-center"><MeshBackground/><div><img src="/heart-icon.svg" className="mx-auto h-24 w-24 animate-heartbeat rounded-[28px] shadow-2xl shadow-pink/30"/><h1 className="display gradient-text mt-6 text-6xl font-bold">Amour</h1><p className="mt-2 italic text-muted">A space for just the two of you</p><div className="mt-8 flex justify-center gap-2">{[0,1,2].map(i=><span key={i} className="h-2 w-2 animate-pulse rounded-full bg-pink" style={{animationDelay:`${i*.25}s`}}/>)}</div></div></div>}
