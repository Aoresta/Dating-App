import { Link } from 'react-router-dom';import { ArrowLeft } from 'lucide-react';import PageLayout from '../components/layout/PageLayout';import DrawingCanvas from '../components/ui/DrawingCanvas';import { useAppStore } from '../store/appStore'
export default function DoodlePage(){const {doodles,addDoodle}=useAppStore();return <PageLayout tabbar={false}><div className="page-enter"><header className="flex items-center gap-3 py-3"><Link to="/home" className="tap rounded-full bg-white/5 p-2"><ArrowLeft size={18}/></Link><div><h1 className="display text-2xl">Doodle Together</h1><p className="text-xs text-muted">Draw a little love note.</p></div></header><DrawingCanvas height={390} onSave={data=>addDoodle({data})}/><h2 className="mt-6 text-sm font-bold">Recent doodles</h2><div className="mt-3 flex gap-3 overflow-x-auto">{doodles.length===0?<p className="text-xs text-muted">Your saved doodles will appear here.</p>:doodles.slice(0,5).map(d=><img key={d.id} src={d.data} className="h-20 w-24 rounded-xl border border-pink/15 object-cover"/>)}</div></div></PageLayout>}
