import {useState} from 'react';import {Link} from 'react-router-dom';import {Moon,Settings} from 'lucide-react';import PageLayout from '../components/layout/PageLayout';import Avatar from '../components/ui/Avatar';import WidgetGrid from '../widgets/WidgetGrid';import {useAppStore} from '../store/appStore'
export default function Dashboard(){const s=useAppStore(),[editing,setEditing]=useState(false),name=s.user?.user_metadata?.full_name||'love',greeting=new Date().getHours()<12?'Good morning':new Date().getHours()<18?'Good afternoon':'Good evening';return <PageLayout><div className="page-enter"><header className="flex items-center justify-between py-3"><div><p className="text-xs text-muted">{greeting},</p><h1 className="display text-3xl">{name}</h1></div><div className="flex items-center gap-2"><Link to="/settings" className="tap rounded-full bg-white/5 p-2 text-muted"><Settings size={18}/></Link><Link to="/profile"><Avatar name={name} size="sm"/></Link></div></header><div className="mb-4 mt-4 flex items-center justify-between"><p className="text-[11px] tracking-[.1em] text-muted">YOUR SPACE</p><button onClick={()=>setEditing(e=>!e)} className={`tap rounded-xl border px-3 py-1.5 text-xs font-semibold ${editing?'border-pink/30 bg-pink/20 text-pink-light':'border-white/10 bg-white/5 text-muted'}`}>{editing?'✓ Done':'✏️ Edit'}</button></div><WidgetGrid isEditing={editing}/><Link to="/lockscreen-doodle" className="glass-card tap mt-4 flex items-center justify-center gap-2 p-4 text-sm font-bold text-pink-light"><Moon size={17}/> 🌙 Lockscreen Doodle</Link>{!s.isPaired&&<Link to="/pair" className="primary-btn mt-5 w-full">💑 Pair with Partner</Link>}</div></PageLayout>}
